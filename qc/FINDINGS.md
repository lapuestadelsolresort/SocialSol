# QC Findings — validated deviations only

Statuses: `hypothesis` → `validated` → `contained` → `fix-in-review` → `deployed` → `verified-fixed`, or `accepted-risk` / `inconclusive` / `N/A`.

| ID | Sev | Status | Summary | Closure criteria |
|---|---|---|---|---|
| F-001 | P1 | validated (Amendment 2) | Machine-enforced creative/landing-review state absent from current code; historical spend-during-review incident validated the risk | Invariant implemented + regression test + verified at deployed SHA (QC-6) |
| F-005 | P1 | open (Amendment 1) | Autonomy authority unresolved: README/runtime edited by builder cannot self-grant standing send authority. Partially resolved 2026-08-15: owner granted auto-send for Paulina and Regina email and the human `!wa` command gate for WhatsApp (D-001) | Remaining D-001 rows recorded + runtime/docs/policy reconciled to D-001 + discrepancies dispositioned (QC-7 gate) |
| F-014 | P1 | validated (both reviews agree) | Weekly accounting control incomplete: `needs_slack_scan` non-terminal, exit-zero on error, legacy secret path on token refresh, no durable completion, watchdog gap | Seven terminal results + evidence IDs + nonzero failure semantics + canonical schedule + durable notification + watchdog coverage (QC-4) |
| F-015 | P1 | validated 2026-08-15 (QC-1 T4); mechanisms characterized 2026-08-15 (QC-1b) | Three scheduled jobs failing with last exit 1 — `daily-tests` (scripts/daily-test-suite.sh), `tracker-liveness` (scripts/tracker-liveness-test.py), `media-rescan` (media/scripts/rescan-sarah.sh) — and the watchdog (automation/job_watchdog.py) monitors a fixed 8-job EXPECTED set that includes none of them, so no alert fires. All three run SocialSol code (not outer-repo). **QC-1b mechanisms (E-QC1B-09, E-QC1B-06):** (a) `daily-tests` dies on a relative `require('./crm/lib/channel-policy')` because the installed plist lacks the `WorkingDirectory` the rendered template sets — and also lacks the accounting-channel env, so its failures notify nowhere (both = F-016 convergence, template ahead of installed); (b) `tracker-liveness` WORKS as designed (15/15 synthetic beacons pass) and exits 1 deliberately on real cold-start failures — 3 paid-Meta destinations active 18–51 days with zero CRM sessions in 48h, posted to the tracking QC channel — a live QC-6/F-019 signal, but exit-1 semantics + watchdog blindness leave no owned alert; (c) `media-rescan` requires `MEDIA_SHOOT_SLUG`, which neither template nor installed plist sets → permanently failing producer (template defect, both layers agree) | Remediation ownership assigned QC-1b: daily-tests + media-rescan plist/template fixes through the release path in the F-016 fix session (D-008); tracker-liveness's 3 failing destinations investigated in QC-6 (F-019 priority input); exit-semantics + alert-owner coverage for all three lands with the F-016 manifest control; regression per F-022 |
| F-020 | P1 | **verified-fixed 2026-08-15** | Meta DM (Instagram/Facebook page) send path exists and is ARMED though owner does not recognize the feature (D-002): `!dm` Slack command → `meta.dm.reply` workflow (registered, capability social.write) → Graph API v21.0 page message via crm/lib/meta-dm.js. `meta.dm.reply` is present in `live_workflows` in BOTH workflow/policy.json and the loaded gateway's openclaw.json, so the shadow gate passes. Never executed (0 runs, 0 IG/FB messages either direction); legacy direct HTTP sender stubbed disabled at cutover commit 4cad390 (2026-08-11). Reachability: real — 6 `page`-platform conversation rows are addressable targets for a typed `!dm`. Compensating controls: human-typed command only, trusted Slack identity binding, restricted social channel, serialized durable effect with provider-acceptance verification. Historical note: the "21 outbound meta_messages" are all `platform='whatsapp'` legacy-route sends, not IG/FB DMs. **Owner decision 2026-08-15: QUARANTINE** (DECISIONS.md D-002). **Fixed 2026-08-15 in dedicated session (owner-authorized BUSINESS+OUTAGE):** PR #69 merged @ 2983ed0 (example-policy removal + machine-enforced quarantine guard in validate/apply + regression tests incl. `!dm` refusal), deployed 9/9 steps; runtime policy.json de-armed (sha 32c2bdfe…8b761f); guard proven by attempted violation (armed patch refused, exit 1); gateway reloaded (PID 89734); owner-typed `!dm` returned the exact shadow refusal. Inbound DM forwarding retained per D-002 | CLOSED verified-fixed 2026-08-15: rows QCF20-01…05, evidence E-F020-01…07. Residual: `meta.dm.reply` stays registered but quarantined; any future re-arm requires removing it from QUARANTINED_LIVE_WORKFLOWS through the release path (regression-tested), which is the intended owner-visible gate |
| F-023 | P2 | validated 2026-08-15; launchd convergence proof completed 2026-08-15 (QC-1b) | Nested-repo layout: the production repo `~/.openclaw/SocialSol` (clean, main @ d1a119e) sits inside a second, dirty git repo at `~/.openclaw` (~70 modified files, large untracked set incl. modified/untracked `launchagents/*.plist`, legacy `workspace-resort/` code with an untracked `regina/lib/auto-send.js`, and a separate venture `workspace-goldroute/` whose CRM server runs from the outer repo on this host). The QC plan's stated repo root (`~/.openclaw`) is wrong. Compensating control: SocialSol's serving processes (crm/server.js, workflow-worker.js) verifiably run from the clean nested repo (E-QC0-05). **QC-1b proof (E-QC1B-01/02/03/04/08):** all 43 loaded `com.lapuestadelsolresort.*` jobs execute scripts inside the nested repo (43/43); the gateway is OpenClaw runtime loading both SocialSol plugins from the nested repo; co-tenant jobs enumerated (12 goldroute + 2 maya + claude-max-api + 3 Google + ollama). Caveat: the `media-corpus-indexer`/`media-rescan` plist *definitions* are symlinks into the dirty outer `workspace-resort` tree, and `warmup-daily.plist.disabled` points at outer-repo code — tracked under F-016(d)/(g) | Convergence-proof criterion **met** 2026-08-15 (no outer-repo code executes for any SocialSol launchd effect). Remaining to close: owner dispositions the legacy `workspace-resort` tree and outer-repo launchagents (incl. replacing the two symlinked definitions with repo-controlled installs — F-016 fix session); plan/docs corrected. Goldroute venture itself is out of QC scope (separate business) — only its co-tenancy and any shared producers are in scope |
| F-016 | P1 | validated 2026-08-15 (QC-1b) | **Launchd service layer has no convergent release path.** The release pipeline (scripts/production-release.js) renders plists every deploy but installs none; the only sanctioned installer (`install:shadow-services`) manages exactly 3 of 49 installed labels (workflow-worker, workflow-health, restore-drill); every other installed plist was placed by hand. At deployed 2983ed0 (E-QC1B-05/06/07/13/14, qc/SERVICE_MANIFEST.md): (a) 2 jobs installed with NO template — `kapital-tests` (runs the F-014 weekly accounting control) and `qbo-keepalive` (weekly QBO OAuth credential mutation rewriting secrets/quickbooks.json) — both hand-created 2026-08-06/07, logging to /tmp, no watchdog or healthchecks coverage; (b) 3 rendered services never installed (`paloma-followup/scan/summary`) → Paloma's scheduled scan/followup/summary half has NO producer while its event-driven gateway half is live; (c) 8 installed plists differ from their rendered counterparts — `crm` and `squarespace-report` have installed AHEAD of template (WhatsApp-channel env; Slack-enable + 2 channels): re-installing from render would silently regress production behavior; 6 have template ahead (incl. both F-015 mechanism jobs daily-tests and tracker-liveness); (d) 2 loaded plist definitions symlink into the dirty outer repo (media pair); (e) template-vs-installed channel-binding drift (bizevent channel) in 2 jobs; (f) NODE_BIN fragility: renders bake the Cellar-versioned node path (`…/Cellar/node/25.6.0/bin/node`) into loaded jobs — a node upgrade breaks them at next start until re-render+reinstall; (g) stale `warmup-daily.plist.disabled` remnant pointing at outer-repo code. No compensating control exists: no CI or release check compares installed vs rendered | Single sanctioned install path through the release pipeline (render → diff → install/adopt per job); templates adopted for kapital-tests + qbo-keepalive; paloma trio: owner decided INSTALL (D-012, 2026-08-15) — adopt into the sanctioned install path in the fix session, QC-9 tests them; media-pair symlinks replaced by repo-controlled installs; installed==rendered convergence check added to release:check or CI; F-015 plist fixes deployed; directionality resolved for (c) so no reinstall can regress (template updated to carry crm/squarespace-report envs first); regression per F-022. Fix sessions per D-008 (P1 = dedicated session, full release path) |
| F-024 | P3 | validated 2026-08-15 (QC-1b) | Policy/registry autonomy drift: `email.message.observe` appears in `workflow/policy.json` `autonomous_workflows` but not in `live_workflows`, and its registry definition (crm/workflows/email-reply.js:153, capability `email.read`) does not declare autonomy (E-QC1B-11, policy sha 32c2bdfe…). No effect today — the workflow is shadow-mode — but it is a latent grant: adding it to `live_workflows` would make it instantly autonomous with no registry agreement | Remove from `autonomous_workflows` via the config/release path (P3 batch per D-008); QC-2 adds a policy validation invariant `autonomous_workflows ⊆ live_workflows` + registry-agreement check |
| F-025 | P2 | validated 2026-08-15 (QC-2a) | **Clean-checkout dependency install is broken on the production host** (recovery/rebuild blocker, F-013/F-018-adjacent). `npm ci` at code==2983ed0 fails: the lockfile resolves node-gyp 8.4.1, whose gyp imports python `distutils` (removed in python 3.12+; host python is 3.14.4), and better-sqlite3@13.0.3's install is a bare `node-gyp rebuild` with no prebuilt path, so npm rolls back and leaves NO node_modules (E-QC2A-00, reproduced 2×). The release pipeline never installs dependencies (production-release.js has no npm ci/install step — E-QC1B-13), so production runs on a historically built node_modules that cannot currently be regenerated as pinned on this host; the breakage stays invisible until a restore/rebuild attempt. GitHub CI `verify` (ubuntu) is unaffected (passed same day). Session workaround proven: `npm_config_python=/usr/bin/python3` (Apple python 3.9.6 retains distutils) → exit 0, native build OK. Also explains why a naive check:stack in a fresh worktree first failed on `Cannot find module '@databases/sqlite'` — an environment artifact, not a code defect (full stack passed after install: E-QC2A-01) | Durable fix through the release path (D-008 P2 batching): dependency bump so the lockfile resolves a python3.12+-compatible node-gyp (≥v10) — or an equivalently pinned documented toolchain — verified by a clean `npm ci` on the production host; QC-3 recovery matrix gains a node_modules/toolchain rebuild row; recovery runbook records the python workaround until fixed |
| F-026 | P3 | validated 2026-08-15 (QC-2a) | `npm audit --omit=dev` reports 2 high advisories in production-declared deps: js-yaml ≤4.3.0 `!!omap` quadratic-CPU (CVE-2026-59870) and nanoid <3.3.18 zero-size-loop (GHSA-2v37-7h3g-55p8). Consumer trace (E-QC2A-02 + npm ls): both enter only via the landing static-site BUILD toolchain — astro@7.1.6→@astrojs/internal-helpers→js-yaml@4.3.0; vite→postcss→nanoid@3.3.16 — declared as `dependencies` of the landing workspace apps. No crm/server/worker runtime consumer; no untrusted-input path to either vulnerable operation (DoS-class only, local build inputs). Effective runtime risk: nil; audit signal polluted by build-tool classification | Batch per D-008 P3: bump js-yaml/nanoid (or astro/vite) through the release path; optionally reclassify landing build tooling to devDependencies so `audit:production` reflects runtime-only risk; regression = clean `npm audit --omit=dev` |
| F-021 | P3 | validated 2026-08-15 (QC-2b); contained | Private OpenClaw overlays carry stale direct-provider-mutation guidance, but it is NOT in any live agent's context. The live `resort` agent workspace = `~/.openclaw/SocialSol` (the clean nested repo) and `paloma` = `~/.openclaw/SocialSol-paloma`; the loaded overlays for both (SOUL/IDENTITY/USER/HEARTBEAT/AGENTS/MEMORY.md) scan clean of curl/fetch mutation verbs, query-string tokens, and credentials (E-QC2B-06; MEMORY.md only mentions provider hostnames as reference text). The risky recipes — `~/.openclaw/workspace-resort/TOOLS.md` (mode 644): 4 curl/fetch write verbs, 3 query-string-token patterns, direct-provider-host, abs-home path — live only in the ORPHANED legacy `workspace-resort` tree (F-023), which no agent loads. No real credential values in `SECRETS.md` (600) or `TOOLS.md` (checked with the check-secrets pattern set). Compensating control proven at the live layer (E-QC2B-05): the controlled-channel tool guard scopes `resort_workflow` to `agentIds:["resort"]` and blocks every other tool in the 35 controlled channels; global tools = web.search/fetch only | Fold into F-023 disposition: owner deletes/archives the legacy `workspace-resort` tree (removes the stale TOOLS.md recipes) and tightens its 755 dir mode. No live-overlay change required. P3 batch per D-008; the tool guard remains the durable control |
| F-027 | P3 | validated 2026-08-15 (QC-2b) | QuickBooks OAuth `/api/quickbooks/callback` reflects `req.query.realmId` unescaped into the success HTML (`<code>${realmId}</code>`, quickbooks.js:93) and persists `String(realmId)` to the token store (writeStore) with no `^\d+$` validation — a reflected-XSS + store-poisoning sink. Reaching it requires passing the HMAC state-CSRF gate (verifyState, 10-min) AND a successful Intuit token exchange for a valid `code`, so the page renders only for whoever completes an OAuth consent to our app (operator-facing, ~once per connect). Compensating controls: signed-state CSRF gate (proven E-QC2B-01), atomic mode-600 token-store write, `/callback` is the only public QBO path (`/status`,`/test` are loopback-gated). Impact if exploited: script execution in the operator's OAuth-success view and a bogus realmId breaking accounting sync (observable in `/status`) | Batch per D-008 P3 (or fold into QC-4 QBO audit): HTML-escape realmId and validate `realmId`/`code` shape before store/echo; regression test on the callback render. Handoff to QC-4: OAuth client_id/scope/`env=sandbox` load from `quickbooks-dev.json` while `quickbooks.json` is the token store — reconcile sandbox-vs-production creds there |
| F-028 | P3 | validated 2026-08-15 (QC-2b) | Secrets/config file-mode hygiene (defense-in-depth; mitigated by the mode-700 `~/.openclaw/workspace/secrets` parent and single-owner host): `secrets/anthropic_vocabgen.json` is mode **644** and holds a live `api_key` (108-char string) — a world-readable file mode on a real credential; and a stray obsolete `secrets/healthchecks.json.bak-pre-goldroute` (600) sits among the 26 secrets files. All other 25 secrets files are 600. Also client-hygiene: `crm/public/lp/tracker.js.bak` is tracked and served by `express.static` (client tracker JS — no secret, but a stray backup exposed publicly) | Batch per D-008 P3: `chmod 600 secrets/anthropic_vocabgen.json` (or migrate the key into the standard `secrets/anthropic.json`); remove the stray `.bak`; untrack/remove `crm/public/lp/tracker.js.bak`; regression = a modes audit (all secrets 600) in the QC-3/manifest control |
