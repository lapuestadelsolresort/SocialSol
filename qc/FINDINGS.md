# QC Findings — validated deviations only

Statuses: `hypothesis` → `validated` → `contained` → `fix-in-review` → `deployed` → `verified-fixed`, or `accepted-risk` / `inconclusive` / `N/A`.

| ID | Sev | Status | Summary | Closure criteria |
|---|---|---|---|---|
| F-001 | P1 | validated (Amendment 2); re-verified absent at 2983ed0 2026-08-16 (QC6-04, E-QC6-03) | Machine-enforced creative/landing-review state absent from current code; historical spend-during-review incident validated the risk. QC-6 re-verification: zero code consumers of the brief's prose `activation_gate`; no review-state identifier anywhere in code. Compensating geometry mapped: provision always PAUSED (code + readback), activation confirmation-gated with full brief↔live semantic assert (`compare_brief_to_live`: name/objective/budget/goal/geo/ages/platforms/detailed targeting/custom audiences/advantage flag/relaxation/creative links), landing status+weight changes confirmation-gated with transactional before-value recheck; CLI activate additionally enforces the designated-approver Slack receipt (campaign_approval.py) and auto-rolls back to PAUSED on post-activation drift, while the graph path enforces same-proposer confirm without the receipt and routes readback failure to manual review without auto-rollback (both asymmetries = fix-spec inputs; approver identity also feeds the open D-001 Meta-activation row) | Invariant implemented + regression test + verified at deployed SHA (QC-6). **Scope raised by D-001 (2026-08-16): the owner authorized autonomous activation with INVARIANT-FIRST sequencing — the fix session lands the review-state invariant (+ committed briefs per F-045) BEFORE arming autonomous activation, since the human `!meta confirm` backstop being removed is currently the only activation gate** |
| F-005 | P1 | open (Amendment 1); **D-001 FULLY RECORDED 2026-08-16 (owner sign-off, session 13 continuation) — QC-7 gate satisfied** | Autonomy authority unresolved: README/runtime edited by builder cannot self-grant standing send authority. 2026-08-15: owner granted Paulina/Regina email auto-send + human `!wa` WhatsApp gate. 2026-08-16: all seven remaining rows recorded (Meta activation autonomous with invariant-first arming; per-campaign budget increase autonomous under ratified $80/day owner-only aggregate cap; pause/decrease ratified; QBO, OwnerRez, Postiz-sanctioned-surface, Regina Airbnb-thread [grant-only], Sarah replies both providers = full autopilot). Grants exceeding built confirm-gated surfaces = recorded intent with arming via release path, NOT silently armed | Runtime/docs/policy reconciled to the complete D-001 matrix + every discrepancy dispositioned (QC-7); arming fix-list items executed per D-008 |
| F-014 | P1 | validated at 2983ed0 2026-08-16 (QC-4a static audit, E-QC4A-06; deliberately not executed — running it performs live QBO reads + an unlocked token refresh) | Weekly accounting control (`kapital-tests` Mon 08:00 → accounting/run_weekly_tests.sh → tests.py) incomplete, all prior claims CONFIRMED + sharpened: (a) 2 of 7 checks (receipt_channel_coverage, mayela_checkmark) return `needs_slack_scan` with instructions addressed to the retired "Sol" orchestrator — no consumer exists, permanently non-terminal; (b) per-test exceptions → status ERROR but `__main__` always exits 0 (only a pre-loop auth crash propagates) — launchd records success on check failure; (c) the "Slack-ready report" goes only to /tmp/kapital-tests.log — no Slack post, no Healthchecks ping, no watchdog label (F-016a job); (d) tests.py `_qbo_auth` does its own token refresh: path HARD-ANCHORED to repo secrets (ignores SOCIALSOL_SECRETS_DIR env contract), NO `.refresh.lock`, NON-ATOMIC plain rewrite of quickbooks.json (crash mid-write corrupts the only credentials file) — the "legacy secret path" claim in its current form; (e) NEW: `test_duplicates` queries without pagination/MAXRESULTS → QBO default page (100 rows) silently caps a 3-month window while reporting `total_purchases_scanned` as if complete; tests 3/4/5/7 cap at MAXRESULTS 200/500 unpaginated; (f) NEW: test 5 hard-codes salary workers + expected amounts in code, ignoring config `salary_patterns` (config/code drift inside the control) | Seven terminal results + evidence IDs + nonzero failure semantics + canonical schedule (template + manifest row, with F-016) + durable notification + watchdog coverage + paginated full-window queries + token refresh through the sanctioned locked/atomic path (or no refresh in the test job at all) + salary matrix from config (QC-4b defines the rebuild spec; fix session per D-008) |
| F-015 | P1 | validated 2026-08-15 (QC-1 T4); mechanisms characterized 2026-08-15 (QC-1b) | Three scheduled jobs failing with last exit 1 — `daily-tests` (scripts/daily-test-suite.sh), `tracker-liveness` (scripts/tracker-liveness-test.py), `media-rescan` (media/scripts/rescan-sarah.sh) — and the watchdog (automation/job_watchdog.py) monitors a fixed 8-job EXPECTED set that includes none of them, so no alert fires. All three run SocialSol code (not outer-repo). **QC-1b mechanisms (E-QC1B-09, E-QC1B-06):** (a) `daily-tests` dies on a relative `require('./crm/lib/channel-policy')` because the installed plist lacks the `WorkingDirectory` the rendered template sets — and also lacks the accounting-channel env, so its failures notify nowhere (both = F-016 convergence, template ahead of installed); (b) `tracker-liveness` WORKS as designed (15/15 synthetic beacons pass) and exits 1 deliberately on real cold-start failures — 3 paid-Meta destinations active 18–51 days with zero CRM sessions in 48h, posted to the tracking QC channel — a live QC-6/F-019 signal, but exit-1 semantics + watchdog blindness leave no owned alert; (c) `media-rescan` requires `MEDIA_SHOOT_SLUG`, which neither template nor installed plist sets → permanently failing producer (template defect, both layers agree) | Remediation ownership assigned QC-1b: daily-tests + media-rescan plist/template fixes through the release path in the F-016 fix session (D-008); tracker-liveness's failing destinations investigated in QC-6 2026-08-16: now 2 (one recovered), both explained as Meta ad-level delivery skew, mechanism recorded in F-019 (validated); tracker-liveness still exits 1 and remains watchdog-blind; exit-semantics + alert-owner coverage for all three lands with the F-016 manifest control; regression per F-022 |
| F-020 | P1 | **verified-fixed 2026-08-15** | Meta DM (Instagram/Facebook page) send path exists and is ARMED though owner does not recognize the feature (D-002): `!dm` Slack command → `meta.dm.reply` workflow (registered, capability social.write) → Graph API v21.0 page message via crm/lib/meta-dm.js. `meta.dm.reply` is present in `live_workflows` in BOTH workflow/policy.json and the loaded gateway's openclaw.json, so the shadow gate passes. Never executed (0 runs, 0 IG/FB messages either direction); legacy direct HTTP sender stubbed disabled at cutover commit 4cad390 (2026-08-11). Reachability: real — 6 `page`-platform conversation rows are addressable targets for a typed `!dm`. Compensating controls: human-typed command only, trusted Slack identity binding, restricted social channel, serialized durable effect with provider-acceptance verification. Historical note: the "21 outbound meta_messages" are all `platform='whatsapp'` legacy-route sends, not IG/FB DMs. **Owner decision 2026-08-15: QUARANTINE** (DECISIONS.md D-002). **Fixed 2026-08-15 in dedicated session (owner-authorized BUSINESS+OUTAGE):** PR #69 merged @ 2983ed0 (example-policy removal + machine-enforced quarantine guard in validate/apply + regression tests incl. `!dm` refusal), deployed 9/9 steps; runtime policy.json de-armed (sha 32c2bdfe…8b761f); guard proven by attempted violation (armed patch refused, exit 1); gateway reloaded (PID 89734); owner-typed `!dm` returned the exact shadow refusal. Inbound DM forwarding retained per D-002 | CLOSED verified-fixed 2026-08-15: rows QCF20-01…05, evidence E-F020-01…07. Residual: `meta.dm.reply` stays registered but quarantined; any future re-arm requires removing it from QUARANTINED_LIVE_WORKFLOWS through the release path (regression-tested), which is the intended owner-visible gate |
| F-023 | P2 | validated 2026-08-15; launchd convergence proof completed 2026-08-15 (QC-1b) | Nested-repo layout: the production repo `~/.openclaw/SocialSol` (clean, main @ d1a119e) sits inside a second, dirty git repo at `~/.openclaw` (~70 modified files, large untracked set incl. modified/untracked `launchagents/*.plist`, legacy `workspace-resort/` code with an untracked `regina/lib/auto-send.js`, and a separate venture `workspace-goldroute/` whose CRM server runs from the outer repo on this host). The QC plan's stated repo root (`~/.openclaw`) is wrong. Compensating control: SocialSol's serving processes (crm/server.js, workflow-worker.js) verifiably run from the clean nested repo (E-QC0-05). **QC-1b proof (E-QC1B-01/02/03/04/08):** all 43 loaded `com.lapuestadelsolresort.*` jobs execute scripts inside the nested repo (43/43); the gateway is OpenClaw runtime loading both SocialSol plugins from the nested repo; co-tenant jobs enumerated (12 goldroute + 2 maya + claude-max-api + 3 Google + ollama). Caveat: the `media-corpus-indexer`/`media-rescan` plist *definitions* are symlinks into the dirty outer `workspace-resort` tree, and `warmup-daily.plist.disabled` points at outer-repo code — tracked under F-016(d)/(g) | Convergence-proof criterion **met** 2026-08-15 (no outer-repo code executes for any SocialSol launchd effect). Remaining to close: owner dispositions the legacy `workspace-resort` tree and outer-repo launchagents (incl. replacing the two symlinked definitions with repo-controlled installs — F-016 fix session); plan/docs corrected. Goldroute venture itself is out of QC scope (separate business) — only its co-tenancy and any shared producers are in scope |
| F-016 | P1 | validated 2026-08-15 (QC-1b) | **Launchd service layer has no convergent release path.** The release pipeline (scripts/production-release.js) renders plists every deploy but installs none; the only sanctioned installer (`install:shadow-services`) manages exactly 3 of 49 installed labels (workflow-worker, workflow-health, restore-drill); every other installed plist was placed by hand. At deployed 2983ed0 (E-QC1B-05/06/07/13/14, qc/SERVICE_MANIFEST.md): (a) 2 jobs installed with NO template — `kapital-tests` (runs the F-014 weekly accounting control) and `qbo-keepalive` (weekly QBO OAuth credential mutation rewriting secrets/quickbooks.json) — both hand-created 2026-08-06/07, logging to /tmp, no watchdog or healthchecks coverage; (b) 3 rendered services never installed (`paloma-followup/scan/summary`) → Paloma's scheduled scan/followup/summary half has NO producer while its event-driven gateway half is live; (c) 8 installed plists differ from their rendered counterparts — `crm` and `squarespace-report` have installed AHEAD of template (WhatsApp-channel env; Slack-enable + 2 channels): re-installing from render would silently regress production behavior; 6 have template ahead (incl. both F-015 mechanism jobs daily-tests and tracker-liveness); (d) 2 loaded plist definitions symlink into the dirty outer repo (media pair); (e) template-vs-installed channel-binding drift (bizevent channel) in 2 jobs; (f) NODE_BIN fragility: renders bake the Cellar-versioned node path (`…/Cellar/node/25.6.0/bin/node`) into loaded jobs — a node upgrade breaks them at next start until re-render+reinstall; (g) stale `warmup-daily.plist.disabled` remnant pointing at outer-repo code. No compensating control exists: no CI or release check compares installed vs rendered | Single sanctioned install path through the release pipeline (render → diff → install/adopt per job); templates adopted for kapital-tests + qbo-keepalive; paloma trio: owner decided INSTALL (D-012, 2026-08-15) — adopt into the sanctioned install path in the fix session, QC-9 tests them; media-pair symlinks replaced by repo-controlled installs; installed==rendered convergence check added to release:check or CI; F-015 plist fixes deployed; directionality resolved for (c) so no reinstall can regress (template updated to carry crm/squarespace-report envs first); regression per F-022. Fix sessions per D-008 (P1 = dedicated session, full release path) |
| F-024 | P3 | validated 2026-08-15 (QC-1b) | Policy/registry autonomy drift: `email.message.observe` appears in `workflow/policy.json` `autonomous_workflows` but not in `live_workflows`, and its registry definition (crm/workflows/email-reply.js:153, capability `email.read`) does not declare autonomy (E-QC1B-11, policy sha 32c2bdfe…). No effect today — the workflow is shadow-mode — but it is a latent grant: adding it to `live_workflows` would make it instantly autonomous with no registry agreement | Remove from `autonomous_workflows` via the config/release path (P3 batch per D-008); QC-2 adds a policy validation invariant `autonomous_workflows ⊆ live_workflows` + registry-agreement check |
| F-025 | P2 | validated 2026-08-15 (QC-2a); **materially corrected 2026-08-15 (QC-3b)** | **Bare `npm ci` fails on the production host, but the sanctioned release path carries a working install step** — two QC-2a claims corrected by QC-3b evidence (E-QC3B-09/09b + deploy record): (1) production-release.js **does** have a dependency step — `install_dependencies` runs `npm ci` with **`PYTHON=/usr/bin/python3` baked into the step env** (lines 226–228), exercised at every deploy; the 2983ed0 deploy fully regenerated node_modules (mtimes 14:39:26–27, 5.03s, strict nonzero-exit check in `command()`); (2) better-sqlite3 ships `prebuilds/darwin-arm64.node` inside the package — installs restore the prebuilt binding without a gyp compile on this arch (no `build/Release`, binding loads from prebuilds). What remains true and reproduced (E-QC2A-00, 2×): `npm ci` **without** that env fails — lockfile resolves node-gyp 8.4.1 whose gyp imports `distutils` (gone in python 3.12+; host python 3.14.4) — so any human- or script-invoked bare `npm ci` during recovery hits a confusing hard failure; the workaround depends on Apple CLT python 3.9.6 keeping `distutils`. CI (ubuntu) unaffected | Durable fix through the release path (D-008 P2 batching): lockfile/toolchain bump to a python3.12+-compatible node-gyp (≥v10) verified by a bare `npm ci` on the production host; recovery runbook line "`PYTHON=/usr/bin/python3` required until fixed" (recorded in qc/RECOVERY_MATRIX.md §3); QC-3b recovery matrix carries the rebuild row (done 2026-08-15) |
| F-026 | P3 | validated 2026-08-15 (QC-2a) | `npm audit --omit=dev` reports 2 high advisories in production-declared deps: js-yaml ≤4.3.0 `!!omap` quadratic-CPU (CVE-2026-59870) and nanoid <3.3.18 zero-size-loop (GHSA-2v37-7h3g-55p8). Consumer trace (E-QC2A-02 + npm ls): both enter only via the landing static-site BUILD toolchain — astro@7.1.6→@astrojs/internal-helpers→js-yaml@4.3.0; vite→postcss→nanoid@3.3.16 — declared as `dependencies` of the landing workspace apps. No crm/server/worker runtime consumer; no untrusted-input path to either vulnerable operation (DoS-class only, local build inputs). Effective runtime risk: nil; audit signal polluted by build-tool classification | Batch per D-008 P3: bump js-yaml/nanoid (or astro/vite) through the release path; optionally reclassify landing build tooling to devDependencies so `audit:production` reflects runtime-only risk; regression = clean `npm audit --omit=dev` |
| F-021 | P3 | validated 2026-08-15 (QC-2b); contained | Private OpenClaw overlays carry stale direct-provider-mutation guidance, but it is NOT in any live agent's context. The live `resort` agent workspace = `~/.openclaw/SocialSol` (the clean nested repo) and `paloma` = `~/.openclaw/SocialSol-paloma`; the loaded overlays for both (SOUL/IDENTITY/USER/HEARTBEAT/AGENTS/MEMORY.md) scan clean of curl/fetch mutation verbs, query-string tokens, and credentials (E-QC2B-06; MEMORY.md only mentions provider hostnames as reference text). The risky recipes — `~/.openclaw/workspace-resort/TOOLS.md` (mode 644): 4 curl/fetch write verbs, 3 query-string-token patterns, direct-provider-host, abs-home path — live only in the ORPHANED legacy `workspace-resort` tree (F-023), which no agent loads. No real credential values in `SECRETS.md` (600) or `TOOLS.md` (checked with the check-secrets pattern set). Compensating control proven at the live layer (E-QC2B-05): the controlled-channel tool guard scopes `resort_workflow` to `agentIds:["resort"]` and blocks every other tool in the 35 controlled channels; global tools = web.search/fetch only | Fold into F-023 disposition: owner deletes/archives the legacy `workspace-resort` tree (removes the stale TOOLS.md recipes) and tightens its 755 dir mode. No live-overlay change required. P3 batch per D-008; the tool guard remains the durable control |
| F-027 | P3 | validated 2026-08-15 (QC-2b) | QuickBooks OAuth `/api/quickbooks/callback` reflects `req.query.realmId` unescaped into the success HTML (`<code>${realmId}</code>`, quickbooks.js:93) and persists `String(realmId)` to the token store (writeStore) with no `^\d+$` validation — a reflected-XSS + store-poisoning sink. Reaching it requires passing the HMAC state-CSRF gate (verifyState, 10-min) AND a successful Intuit token exchange for a valid `code`, so the page renders only for whoever completes an OAuth consent to our app (operator-facing, ~once per connect). Compensating controls: signed-state CSRF gate (proven E-QC2B-01), atomic mode-600 token-store write, `/callback` is the only public QBO path (`/status`,`/test` are loopback-gated). Impact if exploited: script execution in the operator's OAuth-success view and a bogus realmId breaking accounting sync (observable in `/status`) | Batch per D-008 P3 (or fold into QC-4 QBO audit): HTML-escape realmId and validate `realmId`/`code` shape before store/echo; regression test on the callback render. Handoff to QC-4: OAuth client_id/scope/`env=sandbox` load from `quickbooks-dev.json` while `quickbooks.json` is the token store — reconcile sandbox-vs-production creds there |
| F-006 | P2 | validated 2026-08-15 (QC-3a) | **CRM DB path contract is asymmetric and unguarded — converged today by defaults alone.** Effective production DB proven singular: `crm/data/crm.db` (144 MB, mode 600, WAL) at one inode held by exactly {crm server, workflow-worker}; every other consumer resolves there via file-anchored defaults; no override exists at any layer (installed plists set no DB vars; both live spawn envs clean; production `.env` holds no path keys — key-names-only read). The weakness (E-QC3A-02/03): (a) JS runtime honors `DB_PATH` *including via the dotenv-loaded repo-root `.env`* (lib/runtime-paths.js loads `.env` before resolution; server.js:126 and ~20 scripts then read `process.env.DB_PATH`), while the backup producer `automation/crm_backup.py` honors a different name `CRM_DB_PATH` and never reads `.env` — one `.env` `DB_PATH` line would silently move the runtime while backups keep protecting the old file (silent RPO→∞); (b) two consumers ignore overrides entirely (`crm/scripts/ownerrez-message-ingest.js:30` hard-anchored, `automation/lp_phase_gate.py:32`) and would fork state under any override; (c) no startup assertion binds backup source == runtime DB. Compensating control: zero overrides anywhere today + single-inode lsof proof | Fix session (D-008 P2 batch): one canonical resolution for all consumers (runtime-paths as the single source, python included, or retire the env split) + a backup-vs-runtime same-file assertion that fails loud + regression test; QC-3b re-verifies backup source identity |
| F-029 | P2 | validated 2026-08-15 (QC-3a) | **Live schema contains elements with no tracked provenance — schema is not reconstructible from code.** CRM DB: generated reverse-oracle sweep of all 1103 live columns against the full DDL corpus (migrations 001–020 + server.js CREATEs + its 9 `addColumnIfMissing` sites + workflow/squarespace builders + seed-variants) finds **12 out-of-band columns (10 names) on `contacts`/`leads`/`outreach_campaigns`**: OwnerRez-linkage family (`contacts.ownerrez_guest_id`, `leads.ownerrez_guest_id`, `leads.ownerrez_inquiry_id`, `contacts.listing_channel`, `leads.listing_channel`), inquiry-analysis family on `leads` (`analysis_summary`, `group_size_mentioned`, `inquiry_category`, `intent_strength`, `referral_source_clue`, `requested_dates`), and `outreach_campaigns.allow_role_emails`; plus **1 out-of-band index** `idx_contacts_ownerrez_guest_id` (never in tracked code at any ref, `git log -S` all). Live consumers depend on them: `ownerrez-message-ingest.js` (loaded daily) selects by `ownerrez_guest_id`; `crm/lib/squarespace-commerce.js` (graph-squarespace-sync, every 300 s) uses `listing_channel`; dormant `ownerrez-sync.js` INSERTs them; `insert-backfill-leads.js`/prospector legacy use the rest. Paloma `tasks.db`: base tables (`tasks`/`task_updates`/`scan_state`) have no tracked builder either — only the two `scan_state` anti-future triggers are code-defined (paloma/lib/monitoring-contract.js) and live-verified. A from-scratch rebuild (migrations + builders on an empty DB) yields a schema on which loaded jobs crash. Compensating control: backups carry schema (restore path unaffected); everything else matches code exactly (tables 68/68, indexes 124/124 + the 1) | Fix session (D-008 P2 batch): adopt the 12 columns + index into a numbered migration; add a paloma schema builder or provisioning migration; add a schema-parity regression (code-built schema == committed manifest) to the suite; until fixed, QC-3b recovery matrix records restore-from-backup as the only valid rebuild path |
| F-030 | P3 | validated 2026-08-15 (QC-3a) | Stray 0-byte SQLite file `crm/data/resort-crm.db` (mode 644, created==modified 2026-08-14 14:31:38 — the pre-QC deploy-burst afternoon). The filename has never existed in tracked code at any ref; zero references in repo, untracked docs, overlays, outer tree, or shell history; no process has ever held it open; never written. Signature (0 bytes, umask-644 vs the app's 600 pattern, create-only) matches a one-off `sqlite3` open-then-quit against a wrongly guessed filename. Inert: no consumer can resolve to it | P3 batch per D-008: `rm` in a fix session; optional prevention = unexpected-`*.db` hygiene check in the manifest/daily control |
| F-031 | P1 | validated 2026-08-15 (QC-3b); **partially contained 2026-08-15 evening (owner-directed rotation)**; offsite decrypt proofs complete 2026-08-16 (QC3B-12) | **UPDATE 2026-08-16 (QC3B-12):** offsite recoverability of BOTH lineages proven end-to-end from the offsite copies themselves — pre-rotation key × (newest pre-rotation artifact + offsite-only 08-14 daily), current key × (fresh 2.50s retrieval of crm-2026-08-16T021807Z, byte-identical to local) — all batteries clean. The escrow gap itself is UNCHANGED: both passphrases still live only on this host. **UPDATE 2026-08-15 ~19:16 PDT (QC3B-11):** owner directed an immediate rotation to an owner-chosen passphrase, supplied in the operator conversation. Executed: old key preserved byte-exact at `…/resort-backup-passphrase.pre-rotation-20260815` (600, same 700 dir — **do not delete**); new passphrase written (600); verified old key decrypts the existing archive, new key correctly fails on it, then full pipeline with new key: `crm_backup.py --force` → verified encrypted artifact `crm-2026-08-16T021807Z` uploaded offsite → drill decrypt+probes clean (E-QC3B-16). Net position: **future artifacts** no longer single-homed *if the owner retains the passphrase off-host/memory* (owner-side). **All 118 pre-rotation artifacts still depend on the old key, which still exists only on this host** — the archive half of this finding is unchanged. Caveats recorded: the new value appeared in the stored operator transcript (reduced secrecy; re-rotation is cheap), and it is a short human-pattern passphrase vs the previous 64-char random (weaker against offline cracking of the Drive-hosted archive). Original finding: **Backup-encryption passphrase is single-homed on the host it protects; the secrets store has no backup/escrow at all — D-006's zero-loss target for secrets/recovery material is unmet.** The AES passphrase (`~/.openclaw/workspace/secrets/resort-backup-passphrase`, 65 B, 600, created Jul 30 = backup-system genesis) is the sole discoverable copy: zero references to the filename anywhere in the repo, no escrow automation, no runbook, and no job touches either secrets dir (E-QC3B-03/04). Consequence: **losing this Mac (or this one file) renders all 87 offsite CRM artifacts undecryptable — off-host RPO collapses to ∞ despite a perfect offsite chain** (QC3B-06/07). Same evidence shows the broader secrets/recovery material (workspace/secrets incl. this key; `SocialSol/secrets/` 26 files incl. the QBO token store) has no backup control; most provider creds are re-issuable via consoles, the passphrase is not. Compensating today: 700 dir chain + single-owner host — availability, not durability | Remaining closure: off-host escrow of the **old** (pre-rotation) key — it guards all 118 pre-2026-08-16 artifacts (`pbcopy < …/resort-backup-passphrase.pre-rotation-20260815` → password manager, separate Terminal, never `!`); owner confirms the new passphrase is stored off-host (manager/memory); documented recovery-material inventory; escrow-retrieval exercised in a D-006 drill; optional archive re-encrypt under the new key would retire the old-key dependency entirely (fix-session task). Until the old-key half closes, F-013 off-host recovery cannot close for pre-rotation history |
| F-032 | P1 | validated 2026-08-15 (QC-3b) | **Every non-CRM store with a D-006 target lacks any scheduled backup control** (F-018's non-CRM half, now per-store): (a) **paloma `tasks.db`** (hot, task/audit state): no producer; one ad-hoc 644 cold copy Aug 13 — RPO ∞ vs ≤24h/≤8h; (b) **workflow/policy.json + openclaw.json**: ad-hoc pre-change `.bak`s from the sanctioned config path only (good at-change coverage) — same-disk, no cadence/offsite, no documented rebuild of the 75 KB multi-venture openclaw.json → off-host RTO ≤2h unmet; (c) **accounting inbox/processed + untracked config.json + statement CSVs**: no control; compensating — receipts re-downloadable from Slack, Kapital/QBO authoritative, fx_cache rebuildable (Banxico); (d) **warmup state/recipients** (untracked, 644): no control — D-006 explicitly permits "accepted loss + re-ramp" as an owner disposition; (e) **media originals 143 GB** on an 84%-full volume: PIPELINE.md mandates "at least one checksum-verified offline backup" but nothing implements or verifies it (photos-cache subset re-syncable from Drive; derivatives rebuildable). Full store-by-store status: qc/RECOVERY_MATRIX.md §1 (E-QC3B-07/08/10) | Per-store closure: backup producers (tiny stores (a)+(b) fit a nightly encrypted job alongside crm_backup — offsite them with the same pipeline) or explicit owner accepted-risk rows (warmup re-ramp; media if owner attests offline copies — ask); media offline-backup verification becomes a scheduled/manifest control; fix sessions per D-008 |
| F-033 | P2 | validated 2026-08-15 (QC-3b) | **Monitoring/alerting drift — the documented alert fabric partly does not exist:** (a) `job_health._ping` is a **silent no-op for all 13 instrumented slugs** — `secrets/healthchecks.json` contains a check id for none of them, so every "script-ping" in SERVICE_MANIFEST alert rows is false (E-QC3B-11 vs job-status slugs); (b) the daily 09:00 **watchdog is the sole alert path** for the 8-slug set (incl. crm-backup and restore-drill) and is itself unmonitored — no record(), no ping, nothing alerts if it stops; it also depends on the gateway+Slack it watches; (c) the external Healthchecks project carries **3 checks DOWN for 16–88 days un-actioned** (gtku-daily since Jun-08 → dormant gtku; orchestrator-autopause since May-21 → retired legacy orchestrator; regina-batch-run since Jul-30 → moved to graph-regina) — stale checks for retired producers and evidence the down-notification destination is not owned; (d) **no disk-capacity monitor exists in any layer while the data volume is 84% full** (72 Gi free, 145 GB media, growing DB/backups); (e) manifest under-states graph-* coverage (workflow-health's scheduled_graph_missing/stale/failed metrics do cover them — correction, not a gap). Compensating today: watchdog functioning (8/8 green), Healthchecks covers daemon/host death externally (crm-server/tunnel/chroma up), workflow-health 300s deep metrics + own Slack alerts | Fix session (D-008 P2): add the 8 job_health slugs to healthchecks.json (crm-backup, restore-drill, workflow-health first) + a watchdog self-ping (dead-man); retire/reassign the 3 stale checks; add a disk-capacity metric (workflow_health or watchdog); correct SERVICE_MANIFEST alert column both directions; optionally extend the weekly drill to pull the offsite artifact periodically (closes the drill's local-only gap, RECOVERY_MATRIX §2; manually exercised 2026-08-16 QC3B-12 — the automation gap itself remains) |
| F-034 | P3 | validated 2026-08-15 (QC-3b) | Backup/file hygiene batch: (a) `backups/git-pre-scrub-20260811.bundle` (9.9 MB, **644**) — a pre-history-scrub git bundle; if the Aug-11 scrub removed sensitive history, the bundle still carries it (outer-700 mitigated); (b) offsite folder holds duplicate-name uploads (crm-2026-07-30 ×3, crm-2026-08-12 ×2 — Drive doesn't dedupe re-runs) and has no remote retention policy (1.06 GB/17 days, unbounded growth — fine for years, policy absent); (c) local backup retention is file-count-based (keep-30), so deploy bursts silently compress point-in-time depth (~2 days after Aug 13–15); (d) `/tmp` data residue: bank-statement CSV `/tmp/kapital-2026-08-03-13.csv` (600) + builder session logs `/tmp/receipt-check-stack*.log`; (e) unrotated `/tmp` logs (chroma.log 5.5 MB growing) — QC1B-03 family; (f) `backups/.DS_Store`; 644-mode ad-hoc DB copies (QC3A handoff family incl. `paloma-tasks.pre-checkpoint-rewind` bak) | P3 batch per D-008: chmod/relocate-or-delete the bundle (owner decides whether pre-scrub history must survive); clean /tmp residue; date-based local retention or higher keep; optional remote retention policy + dedupe; fold 644-copy cleanup into the same session; unexpected-file hygiene check in the manifest/daily control (F-030's prevention line) |
| F-035 | P2 | validated 2026-08-16 (QC-3b, owner-triggered: overnight token-consumption question) | **An entire producer class — OpenClaw gateway cron agent jobs — exists outside the QC inventory and the service manifest, and it is what consumed Claude tokens overnight.** `~/.openclaw/cron/jobs.json` defines 9 jobs (7 enabled: 3 resort/paloma + 4 goldroute; jobs.json mtime Aug-13 16:12, oldest run records Jun-2026), each an agent turn on `claude-opus-4-6` (paloma on sonnet-4-6) — **contradicting E-QC1INV-18 / QC1-INV-08 "no gateway crons"** (inventory miss). Overnight 20:00→07:15 ≈ **6.45M tokens** (E-QC3B-17): (a) **`paloma-all-channel-monitor` every 10 min, 24/7** — 68 runs, ~5.64M tokens/night ≈ **~12M/day standing consumption**, working as configured (67/68 ok); (b) **goldroute `c7-cram-hourly`: 763 of 764 lifetime runs failed at delivery** (`channel_not_found`), last success Jul-15, erroring since Jun-26, **~41.85M tokens spent on discarded output**, failure notifications also undelivered (silent) — goldroute venture scope (F-023 co-tenant) but shared Claude budget; (c) **resort `auto-organic-ig-post` DELIVERED autonomous Instagram posts Aug-7 and Aug-10** — an uninventoried autonomous publishing producer while D-001's publishing row is OPEN; its Aug-13 run was SKIPPED by the controlled-channel tool-guard restriction (compensating control now active). No alert covers cron-run errors (F-033 family) | Inventory/manifest rows for the gateway cron layer (SERVICE_MANIFEST + INVENTORY correction); owner D-002-style disposition of the resort/paloma jobs — esp. ~~auto-organic-ig-post: recognized/authorized?~~ **(c) DISPOSITIONED 2026-08-16 (owner, D-001 walkthrough): RETIRE the auto-organic-ig-post cron** — disable it in a fix session; organic IG posting moves to the sanctioned autonomous `social.*` workflow surface (tool-guard block remains the interim control until retirement lands); and **paloma-monitor cadence/cost ratification** (~12M tokens/day); cron-run error alerting into the F-033 fix; goldroute `c7-cram-hourly` fix/disable flagged to owner (out of QC scope, real spend); QC1-INV-08 correction noted against E-QC3B-17 |
| F-028 | P3 | validated 2026-08-15 (QC-2b) | Secrets/config file-mode hygiene (defense-in-depth; mitigated by the mode-700 `~/.openclaw/workspace/secrets` parent and single-owner host): `secrets/anthropic_vocabgen.json` is mode **644** and holds a live `api_key` (108-char string) — a world-readable file mode on a real credential; and a stray obsolete `secrets/healthchecks.json.bak-pre-goldroute` (600) sits among the 26 secrets files. All other 25 secrets files are 600. Also client-hygiene: `crm/public/lp/tracker.js.bak` is tracked and served by `express.static` (client tracker JS — no secret, but a stray backup exposed publicly) | Batch per D-008 P3: `chmod 600 secrets/anthropic_vocabgen.json` (or migrate the key into the standard `secrets/anthropic.json`); remove the stray `.bak`; untrack/remove `crm/public/lp/tracker.js.bak`; regression = a modes audit (all secrets 600) in the QC-3/manifest control |
| F-036 | P2 | validated 2026-08-16 (QC-4a, E-QC4A-06) | **QBO OAuth refresh — four independent writers of `secrets/quickbooks.json`, one lock implementation:** (1) `accounting/qbo_push.py` QBOClient.refresh_auth — canonical env-aware path, tmp+fsync+os.replace+chmod 600, **fcntl flock on `.refresh.lock` + double-checked reload** (the reference implementation; used by qbo.write/classify-era 401 retries in the worker); (2) `crm/lib/quickbooks-store.js` (server `/api/quickbooks` routes + quickbooks-api consumers) — canonical path, atomic tmp+rename+600 (no fsync), proactive 5-min-buffer refresh, **no lock**; (3) `scripts/qbo-token-keepalive.sh` (Wed 04:00, F-016a non-canonical job) — canonical path, jq>tmp+mv but **chmod 600 only after mv** (umask window on the tmp + replaced file), **no lock**; (4) `accounting/tests.py` (Mon 08:00) — **hard-anchored path ignoring SOCIALSOL_SECRETS_DIR, non-atomic full rewrite, no lock** (also folded into F-014d). All four rotate the refresh token (Intuit grace window bounds the interleaving damage; schedules rarely collide, so P2 not P1). Plus hygiene: `secrets/quickbooks-dev.json` (dev credentials, 600) lives on the production host as a code-supported fallback. No alert exists on refresh failure in any writer (F-033 family) | One sanctioned refresh implementation (or all writers honor the `.refresh.lock` + tmp+fsync+replace+pre-set-mode protocol) + env-contract compliance everywhere + refresh-failure alerting + owner disposition of quickbooks-dev.json on prod; natural fix vehicle = the F-014/F-016 accounting-jobs fix session (D-008); live-refresh observation lands in QC-4b OAuth audit |
| F-037 | P3 | validated 2026-08-16 (QC-4a, E-QC4A-02) | FX config/doc drift (integrity-positive dead code): `accounting/config.json` `fx.fallback_source=exchangerate-api` and README claim a spot-rate fallback, but `fx_rates._fetch_exchangerate_api` is UNREACHABLE from `get_usd_rate` (today-only helper, never called) — actual behavior is Banxico FIX + ≤4-business-day previous-fix fallback + fail-closed raise, which is the CORRECT accounting posture; `fx.cache_dir` config key is likewise unread (cache dir is env/`__file__`-anchored). Config/README promise a weaker contract than the code delivers | Docs/config-only alignment through the release path (drop the unused keys/claims or wire them intentionally); no behavior change wanted — the fail-closed path is the right one |
| F-038 | P3 | validated 2026-08-16 (QC-4a, E-QC4A-03) | Accounting runtime-file hygiene: real Kapital bank statements live as `accounting/sample_june_2026.csv` / `sample_july_2026.csv` (untracked, 600 — no exposure, but "sample" naming invites treating real financial records as disposable fixtures or committing them as test data); plus the known /tmp statement residue (F-034d family). The August statement pair (`august_2026.csv`, `august_2026_updated.csv`) are overlapping exports of the same account kept loose in accounting/ | P3 batch (D-008): rename real statements to a dated `statements/` convention (or archive into inbox/processed flow), keep out of Git (§4.1), fold /tmp cleanup into F-034; optionally add a hygiene line to the weekly control once rebuilt |
| F-039 | P2 | validated 2026-08-16 (QC-4b, E-QC4B-03) | **Legacy-statement re-upload would double-count SPEI fees.** July (and presumably June) QBO records carry SPEI commission/IVA as split lines EMBEDDED in the parent Purchase (proven by line probe: Purchase 2365 = principal 1,450.96 + Bank Fee 0.35 + Bank Fee 0.06; 0 separate-format fee records exist in the July window; Σ deltas 8.55 ≈ Σ expected fees 9.50 USD). The current pipeline pushes fees as SEPARATE records and its fee reconciliation (find_missing_spei_fees) is deliberately independent of principal dedup and only recognizes the new memo format — so re-processing a legacy-format statement with --live (e.g., re-posting the June/July CSV to the live accounting Slack channel, which stages+processes automatically) would create ~60 standalone fee Purchases duplicating embedded fees (~9.50 USD for July). Principal dedup itself held (52/52 correctly EXISTING/held). Magnitude small; trigger plausible (overlapping re-exports are an established operator pattern — august_2026 vs august_2026_updated) | Fee matcher taught the legacy embedded format (detect Bank-Fee split lines inside a matched parent and count them as existing), or a statement-date guard (statements ≤ cutover month excluded from fee auto-push), through the release path with regression fixtures for both formats; interim operational advisory (STATUS): do not re-upload pre-August statements. Fix session per D-008 |
| F-040 | P2 | validated 2026-08-16 (QC-4b, E-QC4B-02/03) | **July 2026 books deltas in QBO (legacy-flow defects, bounded and quantified):** (a) both fx_conversion Transfers recorded at the FIX rate instead of the bank-executed USD amount — Transfer 2391 Jul-22 = 49.33 vs exact 50.00 (−0.67), Transfer 2400 Jul-30 = 7,417.30 vs exact 7,500.00 (−82.70): **Kapital-account USD inflows understated by 83.37 for July**; the modern `exact_transfer_required`/`transfer_amount_usd` invariant now prevents this and fail-closes on contact (observed live: dedup preflight aborts with "no writes attempted"); (b) two guess-class Daniel Business SPEI debits never recorded at all — Jul-13 6,000.00 MXN (~348.23) + Jul-20 3,349.24 MXN (~194.38): **~USD 542.61 of July expenses (plus their fee lines) absent from QBO** (legacy flow had no record-unresolved path; the current one records to Uncategorized Expense with review). Reverse direction clean: 0 unexplained integration-owned QBO rows in the window | Owner/accountant disposition: correct the two Transfers to the executed amounts and record the two missing debits (BUSINESS-class QBO writes through an authorized session or directly by the accountant), then re-run the QC-4b reconciliation enumerator to verify 52/52 MATCHED with 2 intentional skips; alternatively owner accepts the historical variance in writing (accepted-risk row). Either path closes at re-verification |
| F-041 | P1 | validated 2026-08-16 (QC-5, E-QC5-01) | **CUTOVER retirement is not reboot-durable: six legacy producers resurrect at next login.** All six CUTOVER-dormant legacy LaunchAgents (squarespace-sync, ownerrez-sync, orchestrator, gtku, prospector-daily, regina-anniversary) remain installed in ~/Library/LaunchAgents with live schedules (squarespace-sync RunAtLoad=true SI=300; orchestrator SI=300; ownerrez-sync SI=900; gtku 08:00, prospector-daily 08:30, regina-anniversary 09:00 daily) and none are in launchd's disabled overrides. Dormancy was achieved by live bootout only: last boot Aug-7 13:51 precedes the Aug-10 legacy install and Aug-11 cutover, so the not-loaded state has never survived a login. At next reboot/login launchd bootstraps all six alongside their loaded graph replacements → duplicate producers for OwnerRez contact sync and Squarespace commerce sync (idempotent design limits damage to convergence/traceability), and — higher risk — resurrection of legacy autonomous pipelines OUTSIDE the durable boundary, including the regina-anniversary outreach cron with no ownership guard (it exits only on config errors; orchestrator has only a stale-lock check; squarespace-sync.js has none). Violates CUTOVER §3's own rule ("remove the corresponding legacy producer … Never run two producers for the same effect concurrently"). Silent after reboot: every monitor shows green exits; watchdog checks liveness, not exclusivity. Extends F-016 with a distinct mechanism (retirement durability, not install convergence); QC3B-09's reboot-ownership row covered only KeepAlive daemons returning | Fix in the F-016 dedicated session (D-008 P1): remove or persistently disable (launchctl disable) the six via the sanctioned path; add a post-reboot/loaded-set==manifest convergence check so any resurrection is detectable; regression row per F-022 |
| F-042 | P3 | validated 2026-08-16 (QC-5, E-QC5-03) | **owner-cash-flow CLI claims "Canonical, read-only" but opens the CRM DB write-capable and runs unconditional schema-ensure** (crm/scripts/owner-cash-flow.js:79-82; same pattern in squarespace-report preview mode and squarespace-sync --dry-run). No data mutation on production (schema exists → CREATE IF NOT EXISTS no-ops) but the handle takes write locks during "read-only" runs and would create a DB at a wrong path. QC5-05 executed the full lib on a readonly:true handle — proof no write access is needed | P2/P3 batch (D-008): open readonly (or gate ensureSchema) for no-write invocations of owner-cash-flow, report preview, and sync dry-run |
| F-043 | P3 | validated 2026-08-16 (QC-5, E-QC5-01) | **squarespace-report never records outbox delivery failures.** On a failed Slack post the run aborts: rows stay pending and the outbox's attempts/last_error columns are never written (only the success path updates them) — failure evidence exists solely in job stderr. Retry semantics remain correct (at-least-once with deterministic event keys); this is an observability gap on a path that has never fired in production (outbox empty since backfill) | P2/P3 batch (D-008): record attempts+last_error on the failure path |
| F-044 | P2 (downgraded from P1 per D-014) | validated 2026-08-16 (QC-6, E-QC6-03); escalated and **owner-RECOGNIZED same session ("Yes, I run it" — D-014)**: owner-run campaign from an account outside system reach, so not unauthorized spend — a governance blind spot | **Unknown external paid Meta stream landing on the main site, invisible to every governance layer.** page_sessions carry `utm_source=facebook, utm_medium=cpc, utm_campaign=sf_full_resort_march_2026, utm_content=<13-digit numeric ad id>`: 1,210 sessions/30d at 85–197/day sustained since 2026-08-07 (strays since 2026-08-01), 1,174 distinct IPs, 99.9% mobile, referrer instagram.com, avg dwell 4.8s, zero CTA-reach/clicks/conversions. The UTM matches no campaign in the sole ad account the system token reaches (that account's 7d spend is fully attributed to the 5 known campaigns; a direct GET on the ad id returns Graph 100/33 not-reachable) → an ad is actively delivering from an account outside system reach (legacy account per F-010, a Page boost account, or a third party). Registry, budget cap, daily paid report, autonomy, and CAPI are all blind to it (CAPI correctly excludes it — contained on the conversion side). Estimated $600–2,400/30d at typical CPC. No containment possible from this system (no credentials to the spending account) | Recognition recorded (D-014). Remaining closure: owner decides register vs accept — register = registry/alias entry (report + cap + liveness coverage) and/or read access to the spending account for reconciliation; accept = accepted-risk row in writing. Re-verified by a fresh QC read showing the stream attributed (or a recorded acceptance) |
| F-045 | P2 | validated 2026-08-16 (QC-6, E-QC6-01/03) | **The gated Meta mutation catalog and the autonomous safety net govern only $5/day of the $40/day active spend.** 3 of 4 ACTIVE campaigns (weddings, corporate-retreats, retarget-warm/hot — $35/day) and 2 of 2 non-planner PAUSED registry campaigns have no committed brief file; `safe_brief()` fails for them, so `campaign_pause`/`campaign_budget`/`campaign_activate` (human-confirmed) and `meta.campaign.autonomous` (auto-pause/decrease) are all unreachable — including emergency pause. The only mutation paths for the majority of live spend are Ads Manager or the operator CLI. The snapshot builder derives autonomous actions only for committed-brief campaigns and the daily report honestly warns "Autonomy blocked for N live campaign(s)" every day. Compensating: daily read-only reconciler mirrors live state, spend fully attributed, $80 cap enforced on gated increases, tracking-health green | Commit briefs for the three active campaigns (or an owner-decided subset) through the release path so the catalog + the F-001 invariant can bind to them; D-001's open Meta rows record the authority; re-verify by a gated pause preflight succeeding (stubbed execute) per campaign |
| F-046 | P3 | validated 2026-08-16 (QC-6, E-QC6-02) | **Runtime registry `weddings.status='PAUSED'` contradicts live ACTIVE (spending) state and nothing alerts on the divergence.** The local `status` field is stale bookkeeping from the 2026-07-28 campaign split (no committed brief; zero `marketing_change_requests` rows ever → no recorded pause decision was violated). Reporting is unaffected (`group_registry` prefers `meta_effective_status`, mirrored daily), but a field that reads as intent contradicts reality silently | P2/P3 batch (D-008): make the reconciler flag (or repair) `status` vs `meta_effective_status` divergence, or retire the legacy `status` field from registry records |
| F-047 | P2 | validated 2026-08-16 (QC-7a, QC7A-03, E-QC7A-03/05) | **Paulina edit-override send-time carve-out bypasses consent hard-stops (suppression + do_not_contact).** orchestrator.js processSend: when a send-time compliance failure occurs and an `edit_override` compliance_failures row exists for the send (written only by POST /api/drafts/:id/edit when a human's edit trips the CONTENT gate item_6/item_7), ALL failed items are downgraded to advisory (source=send_time_override) and the send proceeds, skipping the pause matrix. Intended (per the code's own comments and spec §2.4) for content-quality items 6/7 only, but the implementation applies it to items 1-5 — including item_1 (suppressions) and item_2 (do_not_contact), which are CAN-SPAM / unsubscribe-honor consent hard-stops. FIXTURE case D reproduced: a suppressed + do_not_contact=1 contact with an edit_override marker was SENT. Reachable: (a) a human edit drops the disclosure substring (natural rewrite → item_6 fail → edit_override row written + auto-approved + scheduled); (b) that contact is suppressed/DNC'd (bounce/complaint/unsubscribe on a prior message, or manual add) before the scheduled send fires. Bounded: one email per override row (row→terminal 'sent', not re-selected); campaign not paused; requires a prior human edit (autopilot composer never writes edit_override); 5 edits ever, all terminal; not currently fired. Under D-001 Paulina is full autopilot — this is the one send-path gap where a human-in-the-loop escape hatch is scoped too broadly | Fix (D-008 P2 batch): scope the send-time carve-out to items 6 and 7 only; items 1-5 (suppression, DNC, weekly cap, unsub token, postal address) always enforce at send time regardless of edit_override; add regression test (suppressed+edit_override → cancelled, not sent). Owner to confirm P2 vs P1 |
| F-019 | P3 | validated 2026-08-16 (QC-6, E-QC6-03) — promoted from hypothesis | **Tracker-liveness beacons don't execute deployed page JS, and its cold-start age uses Meta ad `created_time`, not delivery evidence** (tracker-liveness-test.py:203–253). Both halves confirmed at 2983ed0. Today's 2 cold-start failures (main-site `retarget_video`, villas `corporate_retreats_video/king-suite`) are Meta delivery skew — those ads got 2–4 impressions/$0.03–0.19 in 7d while sibling ads took 2,441–2,506 impressions — not tracking breakage: synthetic beacons pass 14/14 and genuine sessions flow on every delivering destination (latest within the hour). Deployed pages verified shipping px.js?v=8 / sq-tracker.js?v=7 (static fetch). Compensating deeper control: tracking-health's session↔link-click reconciliation is green and gates autonomy. Residual: alarm wording misframes no-delivery as tracking risk; page-JS execution proof needs a browser load (CANARY, D-009) | With the F-015/F-016 fix session: cold-start age from ad-level delivery (first impression / impressions>0), alarm wording split (no-delivery vs no-capture), exit/alert semantics owned; browser-load funnel leg = **dedicated QC-6c CANARY session** (named home per owner 2026-08-16; D-009 recorded — schedulable after QC-7 with session-level CANARY approval + cleanup plan) |
