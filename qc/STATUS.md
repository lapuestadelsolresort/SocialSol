# QC status

Updated: 2026-08-16 (PDT) — session 14. **QC-7a (Paulina outreach) COMPLETE.** Rows QC7A-01…05; evidence E-QC7A-01…05. Stopped at the QC-7a/QC-7b split point per §1/§8; QC-7b (Regina) is the exact next task. Send-path verified end to end: `paulina.daily` (live_workflows, autonomous, worker runs orchestrator LIVE — no --dry-run) sends real Resend email as D-001 grants. **T5 re-confirmed** (`realness_score` absent repo-wide; gate = `email_status='verified'` at compose + re-verify at send). **Orchestrator gate battery 10-case FIXTURE**: suppression / do_not_contact / weekly-cap all cancel + auto-pause + zero provider calls; role & verifier-fail-closed block; Resend 429→stable idempotency key `socialsol-outreach-<id>` no double-send; 500→ambiguous+durable review; paused-state skips; happy-path sends with CAN-SPAM footer (List-Unsubscribe + postal address + signed token). **Resend webhook + /unsubscribe FIXTURE on a REAL server boot** (closes deferred QC2B-11): svix-signed bounce/complaint → suppress+DNC+auto-pause; **tampered-body-under-valid-signature → 401 with zero side effect**; unsigned → 401; unsubscribe HMAC valid→200/suppress, garbage→400. Suites at SHA: test:prospector 20/20 + CRM email 19/19.

**NEW F-047 (P2, owner to confirm P2 vs P1): Paulina edit-override send-time carve-out bypasses consent hard-stops.** When a human's draft edit trips the content gate (item_6 disclosure-pattern / item_7 banned-phrase), an `edit_override` marker is written; at send time the orchestrator then downgrades ALL failed gate items to advisory — including item_1 (suppressions) and item_2 (do_not_contact) — and sends, skipping the pause matrix. Intended (per code/spec) for content items 6/7 only. FIXTURE case D reproduced a send to a suppressed + DNC contact. Bounded (one email/override row, campaign not paused, needs a prior human edit; 5 edits ever, all terminal; not fired). Fix (D-008 P2 batch): scope the carve-out to items 6/7 only; items 1-5 always enforce at send; add regression test.

**Reconciliations this session:** F-004 (clicks-as-replies) RESOLVED CLEAN — `reply_detected_at` only from inbound classification, never the click webhook; live replies=9/clicks=0. F-008 (Block Kit plugin) RESOLVED P3 — not in load.paths nor installs registry; composer uses text-command Pathway B. D-011 — impl non-expiring/same-channel MATCHES ratified intent and canonical README; no stale live doc. F-005 (Paulina slice) — paulina.daily autopilot MATCHED D-001; email.reply auto-send is built-confirm-gated → known arming-pending fix-list item, not a violation.

Authorizations session 14: blank line at start (RO+FIXTURE only) → held. All checks RO (static reads; live CRM DB mode=ro sweep) + FIXTURE (disposable DB built from a read-only live DDL dump; providers stubbed via global.fetch; no network/Slack/healthcheck; CRM server booted as a child on the fixture DB+fixture secrets, port 34567 loopback). Zero production writes; production policy sha 32c2bdfe + prospector state/config untouched (verified). QC writes to worktree qc/**, ~/qc-evidence/QC7/ (700/600), scratchpad. Worktree fixture copies of gitignored prospector/config.json+state.json created then removed. Deferred as out-of-class: planted-contact suppression CANARY (needs D-009 identity + session CANARY approval — used FIXTURE synthetic contacts instead).

## Phase ledger — QC-0…QC-6 COMPLETE including boundaries; QC-7a COMPLETE (runtime baseline 2983ed0; prod main 03fe3fe = PR #75 merge)

## ⚠️ Standing advisory (until F-039 fixed)

**Do not re-post pre-August (June/July) Kapital statement CSVs to the accounting Slack channel.** The live pipeline stages and processes automatically and would create ~60 duplicate standalone fee Purchases (~9.50 USD for July; June similar). August-onward statements are fine (new format, full dedup).

## ⚠️ Standing advisory (until F-041 fixed)

**A reboot/login of the Mac mini resurrects all six retired legacy producers** (squarespace-sync, ownerrez-sync, orchestrator, gtku, prospector-daily, **regina-anniversary outreach**) alongside their graph replacements — the on-disk plists keep live schedules and are not disabled. If an unplanned reboot occurs before the F-016/F-041 fix session: immediately `launchctl bootout` the six labels (or verify loaded set == SERVICE_MANIFEST expected set) before trusting producer exclusivity. (No reboot as of session 13: boot Aug-7 predates cutover.)

## Blockers

1. ~~F-044 owner disposition~~ **RESOLVED in-session (D-014): owner recognizes and runs the campaign → P2.** Residual register-vs-accept decision open (non-blocking).
2. ~~QC-6 boundary~~ **DONE in-session (D-014, QC6-11, E-QC6-04): PR #75 merged, prod main ff'd fbe3b03 → 03fe3fe docs-only.** The qc branch is 1 commit ahead of origin (this boundary-record commit) and rides the QC-7 phase-boundary PR, same pattern as prior phases.
3. ~~QC-7 gate~~ **CLEARED in-session (session 13 continuation): D-001 FULLY RECORDED with owner sign-off 2026-08-16** — Meta activation autonomous (invariant-first arming), per-campaign budget increase autonomous under the ratified $80/day owner-only aggregate cap, pause/decrease ratified as built, QBO/OwnerRez/Postiz(sanctioned surface)/Regina-Airbnb-thread(grant-only)/Sarah-replies(both providers) full autopilot, WhatsApp `!wa` unchanged. F-035(c) dispositioned: retire auto-organic-ig-post cron. **New fix-list items (arming, per D-008 release path): Meta autonomous activation (AFTER F-001 invariant + F-045 briefs), OwnerRez mutation autopilot, Sarah auto-send both providers, auto-organic-ig-post retirement, #qc-scratch channel + policy entries (D-009).** **D-009 RECORDED 2026-08-16** (same continuation): #qc-scratch channel; test email jason@lapuestadelsolresort.com; test phone = owner's personal WhatsApp (supplied at CANARY time, aliased in committed text); **browser-load funnel leg named home = dedicated QC-6c CANARY session** (after QC-7; unblocks QC-7's planted-contact CANARY too). Still open D-rows: D-004, D-007 (validator), D-010 (confirm).

Open P1s: F-001 (fix session; invariant-first per D-001), F-005 (QC-7 reconciliation — Paulina slice done; Regina + remaining transports in QC-7b), F-014, F-015, F-016, F-041, F-031, F-032. Open P2s: F-013, F-023, F-025, F-006, F-029, F-033, F-035, F-036, F-039, F-040, F-045, F-044 (owner-recognized external campaign; register-vs-accept residual), **F-047 (new — Paulina edit-override consent-bypass; owner to confirm P2 vs P1)**. Open P3s: F-024, F-026, F-021, F-027, F-028, F-030, F-034, F-037, F-038, F-042, F-043, F-019, F-046. Hypotheses resolved: F-002, F-003, F-007 (largely; residual in F-019), F-010 (partially; rides F-044), **F-004 (RESOLVED CLEAN — QC-7a)**, **F-008 (RESOLVED P3 — QC-7a)**.

## Next

**QC-7b (Regina reactivation outreach)** — the exact next task (QC-7a complete; stop was at the QC-7a/QC-7b split per §1/§8). Per plan §9 QC-7 (Regina) + D-001: verify the runtime flag/config matches D-001 intent (Regina email auto-send = full autopilot); validate current eligibility SQL against invariants (historical cohort counts 39/10/2, 5-star set, 18-cancelled are hypotheses, NOT expected truth); suppression / provenance / language exclusions (`language='es'` rules as implemented) / dossier facts / already-contacted exclusion; `!sent`/`!skip`/`!defer`; anniversary scheduling with stable idempotency; Resend readback; aggregation; manual-attention paths; every gate holds under autopilot with no per-message approval. **Verify NO send path exists for WhatsApp-provenance and Airbnb-thread contacts** (D-001: WhatsApp `!wa` human invariant; Airbnb-thread grant-only, not built). VIP/feedback campaigns = product backlog unless an enabled requirement + scheduled producer exist. Complete the F-005 reconciliation for the Regina + remaining transports (OwnerRez/Sarah/Meta-activation confirm-gates stricter than granted autonomy = intent-recorded/arming-pending, not violations). Reuse the QC-7a FIXTURE harness pattern (disposable DB from live DDL dump, stubbed providers) — Regina runs on the same durable-workflow + `regina.daily`/`regina.campaign` (operational-jobs.js) surface. Surface F-047 to the owner for P1/P2 adjudication and fix-list placement.

**Then QC-6c (dedicated CANARY session, D-009 recorded):** the browser-load funnel leg — controlled loads per destination incl. the two zero-delivery ad variants (F-019) proving page JS → session/UTM → CTA → wa_ref, then the test-phone WhatsApp leg proving signed inbound → lead/attribution → CAPI eligibility; requires session-level CANARY approval + written cleanup/reconciliation plan (§5) and a suitable window; synthetic-session marking per the testlv- model.

**Fix-session sequencing (owner-agreed 2026-08-16, in-session): all fix sessions DELAY until QC-7 completes.** QC-7 audits the unchanged baseline; the standing advisories (F-041 reboot playbook, F-039 CSV rule) are the compensating controls in the interim, and D-013 already sanctions the F-014 delay. Ordering constraints after QC-7: (1) service-layer bundle (F-016+F-041+F-015+D-012 install, + auto-organic-ig-post retirement + #qc-scratch creation) lands BEFORE QC-9 (Paloma dependency) and soon (F-041 = calendar-time risk); (2) F-020 Meta-DM quarantine + Sarah/OwnerRez arming land BEFORE QC-8/QC-9 audit those surfaces (fix-before-audit → phases audit final config); (3) F-001 session (invariant + F-045 briefs, THEN Meta activation arming) any time — activation stays human-confirmed until it runs (zero exposure); (4) F-014 rebuild + P2/P3 batches + QC-6c slot anywhere after QC-7. Net: QC-7 → service bundle + quarantine + arming → QC-8 → QC-9.

**Exact next command** (start ritual, §8):

```
git -C ~/.openclaw/SocialSol status
```

Expected: clean `main` @ 03fe3fe (docs-only merge commit of PR #75; runtime baseline 2983ed0, newest deploy record 2026-08-15T21:39Z unchanged). Then `cd ~/qc-worktree`, read qc/STATUS.md + qc/DECISIONS.md D-001 + plan §9 QC-7 (Regina), and begin QC-7b. F-041 advisory applies if any reboot occurred (note: the six resurrectable legacy producers include `regina-anniversary` outreach — verify the loaded set excludes it before trusting Regina producer exclusivity). Reuse the QC-7a FIXTURE harness at /private/tmp/.../scratchpad/qc7fix (schema.sql = live DDL dump, driver pattern) as the QC-7b starting point.
