# QC Findings — validated deviations only

Statuses: `hypothesis` → `validated` → `contained` → `fix-in-review` → `deployed` → `verified-fixed`, or `accepted-risk` / `inconclusive` / `N/A`.

| ID | Sev | Status | Summary | Closure criteria |
|---|---|---|---|---|
| F-001 | P1 | validated (Amendment 2) | Machine-enforced creative/landing-review state absent from current code; historical spend-during-review incident validated the risk | Invariant implemented + regression test + verified at deployed SHA (QC-6) |
| F-005 | P1 | open (Amendment 1) | Autonomy authority unresolved: README/runtime edited by builder cannot self-grant standing send authority. Partially resolved 2026-08-15: owner granted auto-send for Paulina and Regina email and the human `!wa` command gate for WhatsApp (D-001) | Remaining D-001 rows recorded + runtime/docs/policy reconciled to D-001 + discrepancies dispositioned (QC-7 gate) |
| F-014 | P1 | validated (both reviews agree) | Weekly accounting control incomplete: `needs_slack_scan` non-terminal, exit-zero on error, legacy secret path on token refresh, no durable completion, watchdog gap | Seven terminal results + evidence IDs + nonzero failure semantics + canonical schedule + durable notification + watchdog coverage (QC-4) |
| F-015 | P1 | validated 2026-08-15 (QC-1 T4) | Three scheduled jobs failing with last exit 1 — `daily-tests` (scripts/daily-test-suite.sh), `tracker-liveness` (scripts/tracker-liveness-test.py), `media-rescan` (media/scripts/rescan-sarah.sh) — and the watchdog (automation/job_watchdog.py) monitors a fixed 8-job EXPECTED set that includes none of them, so no alert fires. All three run SocialSol code (not outer-repo). Evidence: E-QC1-T4, E-QC0-05 | Jobs fixed or retired through the release path; watchdog/service-manifest coverage extended so every critical job has an alert owner (QC-1b service manifest defines the canonical set); regression per F-022 |
| F-020 | P1 | validated 2026-08-15 (QC-1 T7) | Meta DM (Instagram/Facebook page) send path exists and is ARMED though owner does not recognize the feature (D-002): `!dm` Slack command → `meta.dm.reply` workflow (registered, capability social.write) → Graph API v21.0 page message via crm/lib/meta-dm.js. `meta.dm.reply` is present in `live_workflows` in BOTH workflow/policy.json and the loaded gateway's openclaw.json, so the shadow gate passes. Never executed (0 runs, 0 IG/FB messages either direction); legacy direct HTTP sender stubbed disabled at cutover commit 4cad390 (2026-08-11). Reachability: real — 6 `page`-platform conversation rows are addressable targets for a typed `!dm`. Compensating controls: human-typed command only, trusted Slack identity binding, restricted social channel, serialized durable effect with provider-acceptance verification. Historical note: the "21 outbound meta_messages" are all `platform='whatsapp'` legacy-route sends, not IG/FB DMs | Owner decision recorded in DECISIONS.md (D-002 row): either (a) quarantine — remove `meta.dm.reply` from live_workflows via the config/release path and verify the gate refuses (shadow-mode reply), or (b) owner authorizes the feature → QC-8 behavioral testing under its full contract. No behavioral testing before the decision |
| F-023 | P2 | validated 2026-08-15 | Nested-repo layout: the production repo `~/.openclaw/SocialSol` (clean, main @ d1a119e) sits inside a second, dirty git repo at `~/.openclaw` (~70 modified files, large untracked set incl. modified/untracked `launchagents/*.plist`, legacy `workspace-resort/` code with an untracked `regina/lib/auto-send.js`, and a separate venture `workspace-goldroute/` whose CRM server runs from the outer repo on this host). The QC plan's stated repo root (`~/.openclaw`) is wrong. Compensating control: SocialSol's serving processes (crm/server.js, workflow-worker.js) verifiably run from the clean nested repo (E-QC0-05) | QC-1b convergence diff proves no installed/loaded launchd job executes outer-repo code for any SocialSol effect; owner dispositions the legacy `workspace-resort` tree and outer-repo launchagents; plan/docs corrected. Goldroute venture itself is out of QC scope (separate business) — only its co-tenancy and any shared producers are in scope |
