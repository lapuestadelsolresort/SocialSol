# Recovery & monitoring matrix — QC-3b

Generated 2026-08-15 PDT (session 9) at deployed baseline 2983ed0 (docs-only main @ 87a4e6c).
Targets are owner decision D-006 (2026-08-15). Raw evidence: `~/qc-evidence/QC3B/` (700/600).
Aliases per plan §4.1: DRIVE-BACKUP-FOLDER, the resort Workspace owner account, and all
channel IDs / check UUIDs live only in evidence files (E-QC3B-03/05/11).

## 1. Recovery matrix per store (vs D-006 targets)

| Store | Target RPO / RTO | Protection today | RPO status | RTO status | Gaps → finding |
|---|---|---|---|---|---|
| CRM DB `crm/data/crm.db` (incl. campaign registry, QC0-12; incl. `sarah_voice_corpus` that rebuilds Chroma) | ≤24h / ≤4h | `automation/crm_backup.py`: SQLite online-backup API from `mode=ro` handle → `quick_check` → gzip → AES-256-CBC (PBKDF2 200k iters, passphrase file) → offsite upload (gog drive, DRIVE-BACKUP-FOLDER) → local prune keep-30. Daily 03:15 + `RunAtLoad` catch-up + `--force` at every deploy. Weekly Tue 05:30 restore drill + drill at every deploy (decrypt latest LOCAL artifact → temp-dir restore → quick_check + 16 required tables + critical columns + FK + orphan checks; 26h staleness gate). Local: 30 files, all 600 in 700 dir. Offsite: **87 artifacts, 1.06 GB, unbroken 2026-07-30 → 2026-08-15, every daily present incl. locally-pruned ones** (E-QC3B-10) | **MET** — dailies unbroken 17/17 days (log + offsite listing); age at audit 4.2h | On-host: **mechanics proven sub-second** (deploy drill 261ms, validated by benchmark E-QC3B-15: gunzip 144MB 0.078s, quick_check 0.048s) — ≤4h dominated by operator response. Off-host: retrieval proven seconds (QC3B-07); **decrypt+restore timing = Mon 2026-08-17 drill**; **recoverability itself gated on key escrow** | Off-host recovery blocked if host+key lost → **F-031 (P1)**. Local retention is 30 *files* not days (deploy bursts evicted to ~2 days depth Aug 13→15); compensated by full offsite history → F-034 note |
| paloma `paloma/data/tasks.db` (hot: mtime minutes old) | ≤24h / ≤8h | ~~None~~ **QC-9 update 2026-08-18 (session 27):** nightly `state-backup` producer (03:45, `automation/state_backup.py`) captures tasks.db via SQLite online backup into an encrypted archive riding the crm_backup encrypt/upload/prune/job_health pipeline (QCFS1-09). Verified live today: `resort-state-backup` job-status ok, last success 2026-08-18T10:45Z, artifact `state-2026-08-18.tar.gz.enc`, watchdog staleness entry `resort-state-backup` present in the service manifest | **MET** (daily; verified fresh at audit) | On-host restore = decrypt + copy (small DB, minutes); off-host rides the crm_backup offsite pipeline | **F-032 CLOSED 2026-08-17**; QC-9 confirms the row (QC9-08, E-QC9-01) |
| `workflow/policy.json` + openclaw config (`~/.openclaw/openclaw.json`, 75 KB multi-venture) | ≤24h / ≤2h | Ad-hoc pre-change `.bak`s written by the sanctioned config path into `runtime/config-backups/` (9 policy + 6 openclaw snapshots; latest policy bak Aug 13; F-020 pre-change copy separate). Same-disk only; change-triggered, no cadence; no offsite; no documented from-scratch rebuild | On-host ~MET by bak-at-change (config changes only via the release/config path) | On-host: minutes (copy back + validate + apply + reload). **Off-host: UNMET/undocumented** (multi-venture openclaw.json reconstruction is hours+, undocumented) | **F-032 (P1)** |
| accounting inbox/archive (`accounting/inbox/processed` 44K; `accounting/config.json` untracked + 7 ad-hoc baks; `fx_cache`; untracked statement CSVs). Note: no dir named "archive" exists — actual layout is `inbox/processed` | ≤24h / ≤24h | No scheduled control. Compensating: receipt sources re-downloadable from Slack history; Kapital/QBO remain authoritative for the records themselves; `fx_cache` rebuildable from Banxico | **UNMET locally**, partially compensated by providers | Re-pull from providers plausible within 24h but unproven | **F-032 (P1)** row with compensating note |
| warmup state (`warmup/state.json` 251 B, `warmup/recipients.json` 980 B; untracked, 644) | ≤7d / ≤24h **or accepted loss + re-ramp** | **None** | UNMET | re-ramp path exists by design | **F-032** row; owner may disposition as accepted loss + re-ramp per D-006 |
| secrets / recovery material (`~/.openclaw/workspace/secrets/` incl. **the backup passphrase**, 65 B, 600, sole copy; `SocialSol/secrets/` 26 files incl. QBO token store) | **zero loss** / ≤4h | **None.** No backup, no escrow, zero repo references to the passphrase file, no automation touches either secrets dir (E-QC3B-04) | **UNMET — catastrophic edge:** losing the passphrase renders all 87 offsite CRM artifacts undecryptable | ≤4h unmet off-host | **F-031 (P1)** — owner question: does an off-host copy of the passphrase already exist? |
| media originals (`media/originals/` 143 GB on an 84%-full volume) | ≤7d / ≤72h | **None verified.** `media/PIPELINE.md` mandates "at least one checksum-verified offline backup" but nothing implements or verifies it. `photos-cache/` subset re-syncable from Drive (drive.readonly service account); derivatives (proxies/thumbs/renditions) rebuildable from originals | **UNMET on evidence** (owner may hold SD cards/offline drives — attestation needed) | ≤72h plausible only if offline copies exist | **F-032 (P1)** row + owner attestation question |
| Chroma (`chroma-data/` 23 MB) | rebuildable — no targets; verify documented rebuild path | Rebuild path documented in `crm/scripts/chroma-server.sh` header (stop → rm → reload plist → `index-voice-corpus.sh`; ~$0.003 from `sarah_voice_corpus` **inside crm.db**, so CRM backups carry the rebuild source). quick_check ok (QC3A-08) | **MET** (verified as documented; not executed — execution would call the embeddings API) | — | none |
| node_modules / toolchain (F-025 row required by STATUS) | — (rebuild row) | Release step `install_dependencies` = `npm ci` **with `PYTHON=/usr/bin/python3` baked into the step env** (production-release.js:226–228); exercised at every deploy — 2983ed0 deploy fully regenerated node_modules in 5.03s (strict exit check; better-sqlite3 restores from shipped `prebuilds/darwin-arm64.node`, no gyp compile). Bare `npm ci` without that env still fails on host python 3.14 (QC2A, reproduced 2×) | — | Deploy-proven on-host | F-025 **rescoped** (corrections in FINDINGS); runbook line required |

Campaign registry: lives in the CRM DB (QC0-12) — covered by the CRM row, no separate store.

## 2. Cadence-aware monitoring matrix (what actually alerts, per layer)

Alert layers that exist: **(A)** Healthchecks.io external dead-man (14 checks; read via API, E-QC3B-11); **(B)** `memory/job-status.json` + `job_watchdog` daily 09:00 → Slack #social-sol (8 EXPECTED slugs, stale thresholds 15m–8d); **(C)** `workflow_health.py` every 300s — 25 metrics (queue/outbox/lease/effect health, runtime git drift, scheduled-graph missing/stale/failed, schema-migration-required) → Slack ops-alerts channel; **(D)** F-015 trio: failing, alerts nowhere (known P1).

| Producer (slug) | Cadence / catch-up | State at audit | Actual alert coverage | Gap |
|---|---|---|---|---|
| crm-backup (`resort-crm-backup`) | daily 03:15 + RunAtLoad + deploy `--force`; launchd serializes per-label; daily-idempotent | ok; 9/9 runs exit 0 since ~Aug-7 boot; unbroken dailies | B (watchdog 30h) only — **healthchecks ping is a silent no-op: no check id exists for ANY of the 13 job_health slugs** | F-033(a) |
| restore-drill (`resort-crm-restore-drill`) | Tue 05:30 (no RunAtLoad; miss-while-off waits a week) + at every deploy | ok; last success 2026-08-15 14:39 PDT (deploy step, 261ms) | B (watchdog 8d) | Drills **local** artifact only — offsite copy never exercised by automation; F-033(f). Note: Tue 05:30 sits outside D-006's weekday 10:00–16:00 drill window — owner may align the plist or scope D-006 to manual drills |
| job-watchdog | daily 09:00, no RunAtLoad | ok; 8/8 runs exit 0; all 8 slugs green today | **Nothing watches the watchdog** — no record(), no ping, no external check; alert path requires gateway+Slack up | F-033(b) |
| workflow-health (`resort-workflow-health`) | every 300s + RunAtLoad; 1140 runs exit 0 | ok; all 25 metrics 0 at audit | B (15m stale) + its own Slack alerts (C); also the strongest coverage for all graph-* jobs (scheduled_graph_missing/stale/failed) — manifest "Alert: NONE" for graph rows understates reality | **No disk-capacity metric anywhere; volume 84% full** — F-033(d) |
| log-rotation (`resort-log-rotation`) | Sun 02:30 + RunAtLoad; keep-14, >10MB threshold, repo `logs/` + `crm/logs/` only | ok; 7.7M + 12M in scope | B (8d) | `/tmp` set outside scope (chroma.log 5.5MB growing, heartbeats, tunnel, kapital-tests, qbo-keepalive, ownerrez trio — QC1B-03); DST note: a spring-forward Sunday has no 02:30 — run skipped until RunAtLoad/next week, inside the 8d threshold |
| tracking-health / marketing-reconcile / lp-phase-gate / weekly-tracking-audit | daily 06:45 / 06:30 / 08:00, weekly Mon 07:00 | all ok | B (30h/30h/30h/8d) | ping no-op (F-033a) |
| Healthchecks external layer (A) | continuous | crm-server, cloudflare-tunnel, chroma-server, orchestrator-tick (slug adopted by the live control plane — crm/server.js + operational-jobs.js), regina-anniversary-cron, regina-reconciliation-cron, voice-corpus-indexer, warmup-daily: **up** | Catches host death for daemons independently of Slack ✓ | **3 checks DOWN 16–88 days un-actioned**: gtku-daily (since 2026-06-08; dormant gtku job), orchestrator-autopause (since 2026-05-21; retired legacy orchestrator), regina-batch-run (since 2026-07-30; moved to graph-regina) — stale checks for retired producers + evidence the down-notifications aren't owned → F-033(c) |
| F-015 trio (daily-tests, tracker-liveness, media-rescan) | daily / daily / Mon | still last-exit 1 (sweep E-QC3B-12); only failing jobs among 43 loaded | none (outside B; no A) | existing F-015/F-016 (unchanged) |
| WAL / disk | — | crm.db-wal 4.4 MB (healthy checkpointing); crm.db 144.8 MB (+0.5 MB since QC3A); data volume 358Gi used / 72Gi free (84%) | — | no capacity monitor → F-033(d) |
| Clock/TZ | host TZ America/Los_Angeles; no plist sets TZ; StartCalendarInterval fires in local time | — | — | 03:15/05:30 jobs unaffected by DST hour; only the 02:30 Sunday rotation has the skip nuance above |
| Reboot ownership | KeepAlive daemons (crm, worker, chroma, tunnel, gateway) + RunAtLoad set | Aug-7 boot evidence: all returned (chroma/tunnel start Aug 7; backup counter 9 = 8 dailies + 1 boot run) | — | — |

## 3. Runbook notes (recovery)

- **Migration 009 exits 1 BY DESIGN on restored data** containing `reviewed_by_human=1` guest_dossiers (QC3A-07): it is a data guard demanding a backup, not a failed restore.
- **`npm ci` on this host requires `PYTHON=/usr/bin/python3`** (the release step carries it; a human typing bare `npm ci` hits the node-gyp/distutils failure — F-025).
- Restores go **only to disposable copies in scratch space** (D-006). The drill script already conforms (`tempfile.TemporaryDirectory`).
- Offsite retrieval: `gog drive ls --parent <DRIVE-BACKUP-FOLDER> --account <resort Workspace owner account>` / `gog drive download <fileId> --out <scratch>` (config: `secrets/resort-backup.json`; values in E-QC3B-03). Proven 2026-08-15: newest artifact byte-identical to local (sha256 73cfe1a9…80dcb both sides); locally-pruned daily crm-2026-08-14 retrieved intact (`Salted__` header, correct size).
- Decrypt: `openssl enc -d -aes-256-cbc -pbkdf2 -iter 200000 -pass file:<passphrase_file>` → gunzip → `PRAGMA quick_check`.
- **Key lineage after the 2026-08-15 ~19:16 PDT owner-directed rotation (QC3B-11):** artifacts **≤ `crm-2026-08-15T213917Z`** (118 files: all offsite history + pre-rotation local) decrypt ONLY with `…/resort-backup-passphrase.pre-rotation-20260815` (600, same dir — **never delete**); artifacts **≥ `crm-2026-08-16T021807Z`** use the current `resort-backup-passphrase`. Both proven by attempted decrypt (E-QC3B-16).
- **Until F-031's old-key half closes, host loss = pre-rotation history unrecoverable** (old key still single-homed); new-key artifacts recoverable iff the owner retains the current passphrase off-host.

## 4. Remaining drill (D-006 window: Mon 2026-08-17 10:00–16:00 PT)

Saturday session honored the conservative D-006 reading: retrieval + RO only; the timed decrypt/restore waits for the weekday window.

1. Preflight (RO): confirm weekday+window; live-state active-sends check (`mode=ro`: pending/queued outbox rows = 0 due now, no queued runs, no due outreach in the next ~15 min — graph-paulina dispatches every 300s when due), no deploy in progress (no `runtime/production-release.lock`), inside 10:00–16:00 (outside guest quiet hours by construction).
2. Timed off-host-format drill (FIXTURE, scratch only), on the two already-retrieved offsite artifacts in `~/qc-evidence/QC3B/offsite/`: decrypt **with the pre-rotation key file** (both artifacts predate the QC3B-11 rotation) → gunzip → quick_check → QC3A oracle probes; record end-to-end RTO (retrieval[measured 2026-08-15: seconds] + restore + verify) vs ≤4h; verify the **offsite-only** crm-2026-08-14 daily decrypts (proves offsite artifacts + old-key lineage). Optionally repeat on the offsite copy of new-key `crm-2026-08-16T021807Z` for current-key lineage.
3. Record QC3B-12 row; close F-018's remaining half; update FINDINGS/RISK_HYPOTHESES/STATUS; then the QC-3 phase-boundary PR (Amendment 3; owner runs `gh pr merge` via `!`).
